<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- title -->
  <title>{{title}}</title>

  <!-- meta tags -->

  <meta property="og:type" content="website">
  <meta property="og:title" content="{{title}}">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{{title}}">


  {{#if featured_image}}
    <meta property="og:image" content="{{featured_image}}">
    <meta name="twitter:image" content="{{featured_image}}">
  {{/if}}
  
  {{#if description}}
    <meta property="og:description" content="{{description}}">
    <meta name="twitter:description" content="{{description}}">
  {{/if}}

  <!-- styles -->
  
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
  <link rel="stylesheet" href="/static/gstyle.css">
</head>
<body>
 <section class="section">
    <div class="container is-max-desktop"> 
      <div class="content"> 
      {{{body}}}

      <hr />
        <i>On the off chance you'd like to subscribe to my writing, please feel free to use <a href="https://essays.georgestrakhov.com/feed.rss" target="_blank">this RSS feed</a>. Thank you.</i>
      </div>
    </div>
 </section>
</body>
</html>